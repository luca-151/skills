# Verifier

Use:

- `../../../verifiers/image/verify-screenshot-dimensions.md` — confirm PNG is 2360×5112 at DPR 2.
- `../../../verifiers/image/verify-no-fallback-font-rendering.md` — confirm body text isn't rendered with a Times/serif fallback.
- `../../../verifiers/package/verify-output-manifest.md` — confirm each output dir contains `index.html`, `screenshot.png`, `spec.json`.

If those shared verifiers don't exist yet, the human acceptance check in `human-test.md` is authoritative for v1.
