# Verifier

Use these shared verifiers when applicable:

- `../../../verifiers/package/verify-output-manifest.md`
- `../../../verifiers/video/verify-playable-video.md`

Skill-specific pass criteria: `stitch-videos-ffmpeg` must satisfy the output and quality checks in its `SKILL.md`.
