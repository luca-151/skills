# Sample Input

Skill: `stitch-videos-ffmpeg`

Brief:
Create a minimal test artifact for a fictional brand called SampleCo. Target platform is vertical social video unless the skill is text-only. Use a small safe input and avoid real external spend unless integration tests are explicitly enabled.

Constraints:
- Save outputs under `skills/test-runs/<timestamp>/stitch-videos-ffmpeg/`.
- Use dry-run mode for external providers when credentials are missing.
- Return a manifest or written plan that identifies status as pass, skipped, blocked, or needs human review.
