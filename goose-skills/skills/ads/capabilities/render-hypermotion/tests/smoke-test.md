# Smoke Test

PIL kinetic-typography spec/CTA cards (italic skew + 1.08x outline echo + 3D extrusion + slam-with-shake + inversion flash, dark grain BG, static Space Grotesk Bold) + the real-logo PIL end card (base64-decode the brand SVG's embedded PNG → slam motion blur + micro-motion + inversion flash at ~60%) ; center-crop the ONE hypermotion clip 1:1 → 9:16 ; dice into 5-6 decreasing-length segments at beat boundaries ; concat in beat_structure order (open on intro card, alternate segment ↔ spec card, end on CTA + brand end card) → silent master ; explicit-map music mux (`-map 0:v -map 1:a`, separate pass) — 1080x1920, ~20-30s (25s default), 30fps, h264 yuv420p +faststart, AAC 192k.

Pass when the assembly runs to a valid master mp4 with real (non-1kbps) audio, every card readable (echoes ≤1.08x, no frame bleed), the diced segments hold product identity, and the end-card real logo is sharp + micro-moving (not frozen). Assembly (PIL cards + dice/intercut/concat/mux) is FREE. The ONE Seedance 2.0 hypermotion i2v and the ElevenLabs music bed are paid calls that in prod route through create-video-fal / create-music-elevenlabs (proxy-routed, bills the agent) — not a provider SDK's default host. Guard both paid calls with skip-if-exists so a re-run never re-bills.
